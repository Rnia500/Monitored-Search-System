doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title My Website
        link(rel="stylesheet" href="/styles.css") 
    body 
        nav.navbar
            a(href='/') Home 
            a(href='/about') About
            a(href='/contact') Contact    
            a(href='/search') Search 
        main 
            block content  // This block will be replaced by specific content in each page
